export function Props({props}){
    
    // return <div>The passed value is {props.value}</div>
    // return <div>The passed value is {props.value}</div>

    return <div>The passed value is {props.value.name}, {props.value.team}</div>
}